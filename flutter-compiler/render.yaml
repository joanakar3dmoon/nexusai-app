services:
  - type: web
    name: nexusai-flutter-compiler
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: PORT
        value: 10000
    healthCheckPath: /health
